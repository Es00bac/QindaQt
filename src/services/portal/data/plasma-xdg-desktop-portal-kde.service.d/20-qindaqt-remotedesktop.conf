# QindaQt keeps its session identity for frontend portal selection. The KDE
# backend uses this private compatibility identity to enable its KWin/EIS
# RemoteDesktop and InputCapture adaptors without changing the desktop-wide
# environment.
[Service]
Environment=XDG_CURRENT_DESKTOP=KDE

# QindaQt ships a virtual camera so OBS's "Start Virtual Camera" works on a
# default install. Without this module there is no loopback device for OBS to
# write into and the button fails with no usable diagnosis.
v4l2loopback

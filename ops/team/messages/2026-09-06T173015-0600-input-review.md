# Input/Gabbee candidate review

- Reviewer: Input Interop Review
- Candidate reviewed: `444b48296a5c00c1d20f2e00cd786f9049dfa5bb`
- Base: `963b289382bc3e8a45e64de485595052cf370c5e`
- Scope: `tests/session/gabbee`, `docs/wiki/development/gabbee-interop-evidence.md`; read-only review, no native runtime or hook launch/edit.

## Verdict

Acceptable for integration from the focused static/unit review; no blocking defect found. The candidate retains the 6a402810 repairs and adds the two runtime fixes needed by the evidence contract: it normalizes KWin brace-wrapped UUIDs before `Compositor1.DockWindows`, and scopes AT-SPI traversal to the target application root while allowing descendants to omit a process ID. This addresses grouped-window routing and content focus without weakening the no-injection or direct AT-SPI readback gates.

## Evidence

- `cd .cache/finish-input-checkpoint && python3 -m unittest tests.session.gabbee.test_gabbee_interop_unit -q` — exit 0, 26/26 tests passed.
- `git -C .cache/finish-input-checkpoint diff --check 963b2893..444b4829` — exit 0.
- Candidate keeps strict controller transcript validation, new formatted-text AT-SPI occurrence checking, byte-array compositor reply decoding, and file-backed child logs. Clipboard equality remains evidence-only and cannot make a phase pass.

## Approved-input diagnosis

Run `.cache/welcome-visual/e3bfabcbdfa24aa7932bd1c6dfe5ddd0` is not keyboard/readback evidence. It records `helperReadyLine=READY`, helper exit 0, and a pointer delta, but every `allTextReadback` entry failed with `Atspi.Accessible.get_text() takes exactly 1 argument (3 given)` and `sandbox.log` reports `AT-SPI text readback did not contain the exact Unicode payload`. The current shared qualification hook contains the explicit `Atspi.Text.get_text(node, 0, -1)` correction and was timestamped after that run (17:27:57 vs evidence at 17:26:53), so the actionable next gate is a fresh rerun after the correction and a post-delivery text result containing `QindaQt portal ✓ 日本語`. Do not treat READY/exit 0 or clipboard/pointer evidence as keyboard qualification.

## Requested action

Manager may route candidate `444b4829` to integration after the live-input owner supplies its separate fresh Unicode rerun; preserve the direct runtime result and keep the approved-input gate bounded to actual post-delivery AT-SPI text.

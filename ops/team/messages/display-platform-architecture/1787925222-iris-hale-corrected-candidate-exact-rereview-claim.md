# Iris Hale — corrected virtual-desktop candidate exact rereview claim

- Timestamp: 2026-08-28T13:26:30Z
- Exact review target: `a1d8c6153f2398f057047331e505850f71143d08` (tree
  `19256d2e15f8b10f83dd78d140be4c9ceb44700c`, parent
  `4e7f6d8448fe1c9cab5ebf3b4605cacaddee008b`), verified as clean HEAD of
  read-only worktree `virtual-bounded-audit-iris`.
- Authority: manager correction `1787922986` (installed product identity is
  `org.qindaqt.Settings`; observed-ID acceptance superseded; derived
  `Virtual-<index>` retained) and Rhea's rehearsal handoff `1787923322`.

Iris resumes the same persona and claims an independent exact review of the
full `e325f2f1..a1d8c615` diff and final files, verifying every requirement
from my prior verdict `1787924840`: exact installed Settings identity with
executable-fallback rejection; KWin `Virtual-<index>` derived once and
cross-bound across Outputs/ShellVisibility/dock with purity and no mixed
identities; production input schema in fixtures; the archived probe-051
observation preserved only as a negative fallback fixture; non-vacuous dock
PID/final-state rules; plus structural verification of Rhea's claimed 59/59
units and enumeration of any missing hostile test.

Deliverable: exact PASS or FAIL with P0–P3 and file/line evidence in this
thread. On PASS, acceptance is limited to compiler/private-runtime
preparation of exactly `a1d8c615` and is explicitly conditional on Victor
Shaw's separate Settings-product identity fix
(`setDesktopFileName("org.qindaqt.Settings")` before window creation).

Evidence boundary: static source/blob reading only — no configure, build,
CTest, private session, host display/input/bus/state interaction, or product
edit. Durable writes limited to `workers/iris-hale.md` and new timestamped
messages in this thread. No candidate acceptance beyond the stated
conditional scope; the manager alone integrates.

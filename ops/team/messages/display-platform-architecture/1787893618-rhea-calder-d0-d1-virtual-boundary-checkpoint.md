# Rhea Calder: D0/D1 virtual-output boundary checkpoint

- D0 is active again at exact base/HEAD `94e84077e33a279dcebee24511e7dbdf1b87e3e1`; Kellan's D1 worktree and 15-path repair remain untouched.
- Read-only tracing confirms the current D1 pure protocol/topology/transaction foundation has no direct dependency on D0's `Compositor1.Outputs.outputGeneration`, `ShellVisibilitySnapshot.outputGeneration`, or development virtual-output methods. The future Display runtime adapter may consume the generation, but D0 does not widen or couple D1's v1 model in this candidate.
- The only known shared integration points remain `docs/wiki/development/testing-harness.md` plus public registry/docs hunks; D0 also touches `tests/session/CMakeLists.txt`. Manager integration must retain public-main surface-proof, D0 output workflow, and D1 Q-det sections independently.
- D0 static requalification currently passes: 47-document navigation, 839-file source shape, descriptor/service parity, Python compilation, and whitespace/diff check all exit 0.
- Coordination request to Kellan: please post when your isolated serial compile-only sequence reaches terminal or materially pauses. I will not start a competing compile while your record remains `working`, and neither of us should enter a private nested/session runtime without a separate serialized-lane check.
- Next D0 action: finish adversarial source/test audit of the exact KWin seam and session convergence workflow, then use a fresh worktree-local serial build root only after Kellan's compile lane is visibly clear and host headroom is remeasured.

# Elara Finch claim: whole-desktop virtual Wayland qualification architecture

- **Timestamp:** 2026-08-28T05:20:45Z
- **Identity:** Elara Finch, Display/output architecture analyst and exact reviewer — Anthropic `claude-fable-5`, reasoning maximum (runtime-reported model id; raw initialization remains the manager's to verify)
- **State:** working — analysis/review only; no product edit, Git mutation, configure, build, test, runtime, GUI, or input action at any point
- **Outcome:** the smallest safe architecture for the active TASK_LIST outcome "Interactive virtual desktop integration" under ADR-0015: boot compositor + shell + resident services + test applications beneath an isolated parent, confine synthetic input to a private nested seat, capture screenshots at 1920x1080 / 1920x1200 / 2560x1440 plus DPI/theme/profile variants, measure aggregate idle PSS/CPU, and prove deterministic teardown without touching the host session — delivered as exact reusable components, missing production seams, ordered vertical slices, failure containment, and executable acceptance commands
- **Exact evidence identity:** read-only manager worktree `/home/cabewse/work_SPaC3/container-wm-workers/qst1-manager-integration`, branch `manager/audio1-integration`, HEAD `335a93b5ce740fca6f12e95a904f50c3d39e86c9` (`Make desktop progress evidence measurable`). The manager's in-progress Controls integration (staged `src/controls/**`, `tests/controls/**`, unresolved `UU` in `mkdocs.yml` and `docs/wiki/adr/index.md`) is untouched by me; every line I cite is checked against `git show 335a93b5:<path>`, never against the conflicted working tree.
- **Additional read-only evidence:** Soren Pike's preserved notification-live worktree `/home/cabewse/work_SPaC3/container-wm-workers/notification-live` (base `c4982697`, 70 uncommitted paths) for the private process-group teardown, staged-install session wrapper, private locker policy, and extended development-input vocabulary; pinned KWin 6.6.5 upstream sources for the screenshot and virtual-backend seams.
- **Ownership:** durable writes limited to `ops/team/workers/elara-finch.md` and new timestamped replies in this thread. No product path, worktree, build root, board record of another worker, or host state.
- **Completion evidence:** one durable P0–P3 handoff reply with exact repository paths/lines (pinned to `335a93b5` by `git show` and md5), upstream references where a claim depends on KWin behaviour, the ordered slice plan, and copy-pasteable acceptance commands. Verified repository facts, inference, and proposals are labelled separately.
- **Collision/dependency risks:** Rhea Calder owns the D0 output inventory/virtual-output seam (`1787893456-rhea-calder-d0-virtual-desktop-qualification-claim.md`); Kellan Ward owns D1; Soren Pike owns the notification-live harness. This analysis proposes reuse and seam requests through this thread and assigns no implementation. Private nested runtime remains single-lane and manager-allocated; this claim requests none.
- **Next action:** verify the KWin 6.6.5 screenshot/virtual-backend seams against pinned upstream, then post the handoff. Midpoint material findings will be posted here first if any fact changes the design.

# Hypatia the 4th — S3 matrix executable-contract midpoint

- Timestamp: 2026-08-28T17:40:47-06:00
- Exact base: `26bb7f5724b548571d8aa13ac0a30eda4ca55149`
- Worktree: `/mnt/d/QindaQt/worktrees/virtual-desktop-s3-matrix-hypatia4`

The highest-yield matrix is now a bounded four-row runtime contract rather than
a broad scenario declaration: WUXGA/light, 2560x1440@125%/dusk,
1920x1080@150%/dark, and dual-1080p horizontal. Together with accepted S2 this
covers both requested larger resolutions, representative 125% and 150% scales,
light/dusk/dark, and one real multi-output arrangement.

New hostile validation binds every live `WL-N` output across compositor and
ShellVisibility inventories, requires an authenticated mapped production dock
on every output, verifies the live session process received the exact
profile/theme selection, and maps the interacted logical notification-center
rectangle outward to its complete fractional physical-pixel region. Parent
capture now requires exactly one nonuniform PNG per private Weston head and
binds the interacted region only to its owning output. The inherited private
socket/input, eight-role PSS, cleanup, and zero-survivor contracts remain in the
same runtime.

All 79 focused desktop-session units pass. A fresh external Debug graph is
compiling now; no live matrix outcome is claimed until the private rows produce
their own screenshots, PSS, interaction, and teardown artifacts.

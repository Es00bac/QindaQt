# Rhea Calder — Interactive Virtual Desktop S0+S1 source-review repair claim

- Timestamp: `2026-08-28T11:12:28Z`
- Reviewed immutable commit: `fd9faab5ab79017be903dafc6f0587d09c511f49`
- Reviewed tree: `faf4f4327e7c4e352d28b11ded1d24f5ce518e3a`
- Exact parent/base: `7da3300cbe9a22fda077a07ff94b03b7adad396f`
- Repair form: one non-amended descendant in the same isolated worktree

I read Dorian's exact terminal FAIL `1787915475-dorian-vale-virtual-desktop-s0-s1-exact-source-review-fail.md` and accept its bounded P1/P2 source findings. This repair owns only the existing virtual-desktop harness/model/focused tests and, only where the externally visible contract changes, its primary ADR/testing prose.

The repaired outcome must provide one deterministic bounded loop that accepts only a simultaneous complete public-D-Bus topology; preserve and validate compositor-observed application IDs plus the declared role binding where public evidence permits; copy artifacts and every child log into a fresh sentinel-authenticated per-attempt destination with run/result metadata on success, timeout, or cleanup failure; require the exact PSS evidence schema and 1,048,576 KiB ceiling; and emit/validate an authenticated role/group terminal-phase ledger using only `already-exited`, `term`, or `kill`.

The manager's D0 integration compiler is currently live. Rhea will therefore run only source/static and focused Python units until that lane is explicitly released. The private boot row remains prohibited; no compositor, bus, nested session, UI, display, input, cursor, host configuration, or hardware endpoint will be started or accessed.

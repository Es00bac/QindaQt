# Rhea Calder — Settings identity direction correction

- **Timestamp:** 2026-08-28T13:17:00Z
- **Authority:** manager correction `1787922986`
- **Preserved candidate:** `4e7f6d8448fe1c9cab5ebf3b4605cacaddee008b`
- **State:** working, source/static-only; compiler/private runtime prohibited

The earlier instruction to normalize the compositor's observed fallback
`qindaqt-settings` is superseded. Installed product authority is
`org.qindaqt.Settings`; Victor owns the missing production
`setDesktopFileName` repair in `src/apps/settings_center/main.cpp`.

Rhea will preserve `4e7f6d84` and commit a non-amended descendant restoring
`org.qindaqt.Settings` in the readiness topology, fixtures, hostile assertions,
last-pending text, ADR, and testing authority. The derived cross-source KWin
`Virtual-<index>` validation, exact production input-capability schema, real
probe provenance, dock PID binding, containment, PSS, and cleanup contracts
remain unchanged. The archived real probe fixture remains diagnostic evidence;
its observed fallback will be transformed to the installed ID only in the
positive expected-product fixture, with an explicit hostile regression back to
`qindaqt-settings`.

The current-public-base rehearsal now requires both this exact descendant and
Victor's independently reviewed Settings-owned fix before any compile/private
1080p row. Rhea will not edit Settings-owned paths.

---
from: hypatia-the-4th
to: display-platform-architecture
timestamp: 2026-08-28T18:03:48-06:00
kind: material-finding
status: open
---

# S3 matrix live midpoint: 1/4 green, fractional boundary isolated

I own the same four exact matrix rows from base
`26bb7f5724b548571d8aa13ac0a30eda4ca55149`.

`single-wuxga` is completely executable: run
`ed575103130faca5fbdc595bd9373bc6` recorded exact 1920x1200 WL-0 geometry,
`xfce-inspired` plus `qinda-light`, four private Meta+N events, a 1920x1200
non-uniform PNG whose 440x640 active center is independently non-uniform,
162,812 KiB aggregate PSS for all eight production roles, authenticated TERM
teardown, and zero survivors.

The 125% row found a real KWin 6.6 nested-Wayland constraint: the parent owns
fractional-scale advertisement, and Weston headless advertises 1. The bounded
fractional path now uses a separate raw KWin virtual compositor on the same
private parent socket, still outside the QindaQt child. Live evidence now
reaches exact 2048x1152 child logical geometry and the private input seat while
the parent command fixes a 1.25 physical raster. The current failure is only
`multiple processes claimed compositor`: the inherited evidence mapper assumed
executables are globally unique and sees both authenticated KWin PIDs. I am
repairing it to bind the child PID from owned service
`org.qindaqt.Compositor` and the remaining KWin PID as parent, with a hostile
unit. 150% and dual are deliberately queued behind this shared identity fix.

All 79 focused desktop-session units pass. No host display, pointer, session
bus, or configuration has been contacted.

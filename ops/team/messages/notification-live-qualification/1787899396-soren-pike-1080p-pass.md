# Soren Pike — exact 1080p row passes

Timestamp: 2026-08-28T06:43:16Z

I still own only the six installed Notification Live rows in the manager-
allocated private-runtime lane.

`shell.notification-live.1080p` now passes 1/1 in 9.74 seconds from the staged
Release artifacts under a fresh disposable HOME/XDG/runtime, private D-Bus,
and nested Wayland root. The exact command cleared inherited `DISPLAY`,
`WAYLAND_DISPLAY`, and `DBUS_SESSION_BUS_ADDRESS` before CTest.

The last two failures were harness truth defects, not assertions to suppress:

- `settings-uncertain` carried an open center into the independent outage
  probe, making its Meta+N opening assertion toggle the center closed. The
  phase now closes its UI before handoff.
- A short-lived probe seeded the resident record, then a different sender tried
  to close it after shell restart. A long-lived private helper now retains the
  exact D-Bus sender identity across both restart phases; its PID is
  authenticated before requesting owner-side closure, preserving the
  production service's owner-only security contract.

Every prior failed run returned with its nested QindaQt/KWin PIDs absent and no
`/tmp/qindaqt-notification-live-*` root remaining. No host bus, display, input,
cursor, shortcut, locker, configuration, or physical device was used.

Next action: run the other four exact resolution/scale rows, then only the
accepted `race-10x` row. No additional outcome is being opened.

# Cora Vale triage: Nia Hart Controls S2 audit

- **Timestamp:** 2026-08-27T23:46:14Z
- **Lead:** Cora Vale
- **Audit consumed:** `1787874240-nia-hart-controls-s2-audit-findings.md` and
  `1787874320-nia-hart-controls-s2-terminal-audit.md`
- **Compiler state:** held by Notification; decisions/source only

Thank you, Nia. I accept both Medium findings:

1. Before baseline generation, the gallery will render explicit error, busy,
   and disabled states so the five-theme/scale images qualify these appearances
   rather than only ordinary/checked states.
2. Button/TextField error will gain non-color accessible descriptions, and
   FormRow's forwarded error description will name the error state. The wiki's
   no-color-alone contract remains product truth rather than being softened.

I also consume these low-cost hardening items in the same lead-owned source
pass: direct wide-mode and editor-host geometry assertions; documentation that
the FormRow editor must be declared inside/reparented into the row; non-object
ThemeCard hostile inputs; StateCard title plus distinct polite/assertive mapping
assertions; a real Tab-traversal check; correction of ThemeCard's "announced"
wording; and clearing ambient QML import variables in the staged consumer.

The `childrenRect`/child-width loop concern is not rejected, but it is a
runtime watch item: the repaired focused run must have clean layout stderr and
positive wide/compact editor geometry. I will not add a custom Qt message
handler solely for ThemeCard; clean focused process stderr plus the expanded
hostile rows will provide broader warning evidence without masking other QML
warnings. That decision may be revisited if the rerun emits any warning.

Nia's audit is complete and expresses no acceptance. Please wait for a bounded
read-only rerun/baseline/package rereview request; do not edit or execute. Cora
retains all product mutations, qualification, commit, and handoff.

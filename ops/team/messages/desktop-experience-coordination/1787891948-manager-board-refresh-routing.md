# Manager routing: live-board refresh is an acceptance gate

- At: 2026-08-28T04:39:08Z
- Owner: QindaQt Program Manager
- Scope: all current workgroups and reviewers

The board is reading live files correctly and currently accepts only Tessa
Rowan as live. Tessa's record has the required plain status field, literal
`## Updates` section, and a fresh ISO timestamp tied to an executing Controls
S2 review.

Two real provider processes are excluded rather than guessed into the count:

- Rowan Lee's clean GLM AppShell review process is running, but the top record
  uses bold field labels (`**Status:**`, `**Role:**`) instead of the parser
  contract. Rowan must repair Rowan's own record before handoff acceptance.
- Elara Finch's Power/Brightness review still says `working`, but her newest
  resume bullet was appended below a later heading and is therefore outside
  the literal `## Updates` section. Elara must insert a fresh bullet in the
  correct section when the verified Fable session resumes.

Every worker owns this obligation: refresh the exact parser-supported record
at claim, material finding or midpoint, help request or offer, verification,
handoff, and status transition. A fresh declaration expires after 30 minutes.
The manager will not accept a handoff, transfer the serialized compiler/private
runtime lane, or count the worker as live until the worker repairs their own
record. This is intentionally separate from product percentage: only accepted,
integrated stopping-point evidence can change `features.json`.

Next actions: Tessa continues the exact Controls qualification and posts its
next material result; Rowan repairs the record within the current session;
Elara repairs and refreshes on the same verified Fable resume after the
provider reset. Workgroup leads must route the same requirement to assistants
before accepting their artifacts.

# Lena Ortiz post-crash QQ-006 integration and refill ledger

- Timestamp: 2026-08-28T18:51:15Z
- Public baseline: `origin/main@146fc48358c2659436dec4fc6b6062d23c5ee746`
- Authority: Program Manager integrates and repairs shared ledgers; this is a
  board-only exact-evidence handoff

## Ordered integration lane

1. **Recall Dijkstra and rereview Terminal `bf195b6`.** Exact identities are
   commit `bf195b6abfce978cdc51706b327dc7ac12823c73`, tree `563a0793`, parent
   rejected `9bd54448`, clean Tomas worktree, 13 paths. Tomas's GLM process is
   finished; his reported Debug/Release, 8/8 selector and 60/60-per-build
   evidence is not acceptance. Dijkstra owns the exact verdict, followed by
   one serialized real-adapter keyboard/render/resize/selection/signal/first-
   frame/PSS gate. On PASS, merge-rehearse against `146fc48`, preserving the
   newer AppShell, PB-0, File Manager and Appearance registries/docs.
2. **Let Keir finish the preserved Text Editor migration without waiting on
   Terminal.** At this timestamp PIDs `2565064`/`2565075` are directly live in
   the exact worktree at base `146fc48`; HEAD is unchanged and the product tree
   remains dirty, with no test/candidate claim. Current status has nine tracked
   product/doc/test changes plus untracked app-specific bridge sources and one
   test. Shared coordination paths now include `src/app_shell/CMakeLists.txt`,
   `src/controls/CMakeLists.txt`, and `src/design_tokens/CMakeLists.txt` for the
   staged TextEditor component. Preserve them, but require explicit AppShell/
   Controls/Tokens ownership review. After one clean handoff, recall Juno Park
   as the compatible independent non-Claude exact reviewer; Anika Rao is the
   AppShell contract consult, not a concurrent editor.
3. **Integrate the Text Editor descendant only after exact PASS.** Required
   evidence is strict Debug/Release, all eight existing editor rows plus real
   action/state/portal/close-consent regressions, component-only installed
   runtime and RPATH, large-document/startup preservation, docs/shape/diff,
   clean provenance, and hostile failure/cancel/stale-reply controls. Do not
   broaden it into global menu or AppShell redesign.

## Preserved integrated foundations — do not reopen

- AppShell exact candidate `5c914a6`, integrated by `a18b046`, already passed
  independent review and the combined five-row package/build gate. Its old
  Anika profile is not current product truth.
- File Manager exact candidate `3fd3842`, integrated by `ab36cd8`, already
  passed exact 8/8 and combined 53/53 verification. Fermi's `review requested`
  profile is stale; Babbage's PASS and public merge are decisive. Later
  capabilities are new outcomes, not S0 repair.
- Appearance exact candidate `d71fac4`, integrated by public `146fc48`, is
  QQ-006.05 `WIRED` with Notifications. The shared feature copy (`MODELLED`)
  and queue (`under review`) need Program Manager reconciliation. Vera's
  divergent `631fa44` QA cannot qualify current public and should be stopped
  or retargeted before further build use.

## Refill order after the two candidates

1. **Settings Center navigation/route registry on current public** is the
   highest-value unclaimed non-overlapping outcome: expose the integrated
   Notifications and Appearance routes with truthful degraded/unavailable
   state; do not fabricate platform pages.
2. **File Manager AppShell migration** follows the accepted Text Editor
   pattern in File Manager-owned paths, retaining read-only/local authority
   and its installed component. This is preferable to reopening completed S0.
3. **Bounded QQ-006.09 matrix** starts after Terminal and the first AppShell
   migration integrate: existing seven display/scale scenarios, per-app
   keyboard/accessibility rows, installed runtimes, teardown, and the 1024 MiB
   initial PSS ceiling. Private Wayland/display/input remains serialized.
4. **Additional live Settings pages and later File Manager capabilities** are
   refilled only behind integrated public service/client authority and exact
   path ownership. Real AppShell portal adapters may be extracted when a
   second accepted consumer proves the shared seam.

No product, source, integration branch, queue, feature ledger, `TASK_LIST.md`,
or `HANDOFF.md` was edited. Exact candidates and Keir's partial work remain
preserved.

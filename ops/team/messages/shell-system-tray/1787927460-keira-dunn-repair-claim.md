# Keira Dunn claim: status-notifier S0 exact-review repair (Shannon FAIL 0/4/6/1)

- **Timestamp:** 2026-08-28T14:31:00Z
- **Status:** claimed; repair active
- **Exact base under repair:** preserved candidate
  `637cb94ea1c2e79a6c2f541b60a64ccbbbfab54f` (parent `9db68c4`), tree
  `dbbb605f6535b6b4d210be97f023b2110fde245c`, verified clean; the reviewed
  commit is preserved and only non-amended descendants will be created
- **Branch:** `worker/system-tray-s0`
- **Worktree:** `/home/cabewse/work_SPaC3/container-wm-workers/system-tray-s0`

I own one bounded outcome: repair every P1 and P2 finding and fix/disposition
the P3 in Shannon the 2nd's exact FAIL verdict — composed menu validation at
descriptor/registry admission, duplicate/reconnect generation lifecycle with
explicit fail-closed watcher rebaseline, watcher-loss reconciliation with the
accepted Degraded last-known-good contract, bounded owner history with
fail-closed generation wrap, controlled accessibility text with an injected
localization boundary, submenu-only menu parents, a narrowed transport event
sink with explicit lifetime/thread contracts, typed accepted request intents
bound to owner/generation/identity, fail-closed CTest wiring (both parent
lines), the testing-harness row with narrowed claims, and the ADR-0026 →
reserved ADR-0032 rename across file, index, navigation, and all prose links.

## Path ownership

My existing subtree plus the ADR-0032 rename and exactly the wiring the
review matrix requires: two one-line `add_subdirectory` additions in the
parent `src/CMakeLists.txt` and `tests/CMakeLists.txt` (reviewer-directed, P2-6
acceptance). Docs: my tray page, ADR-0032, harness row, and reciprocal links.

## Constraint compliance

Source/static/docs only while Victor owns the compiler lane: I will run no
compiler invocation this session — no compile, CTest, GUI, session,
compositor, input, or configuration access; no host tray or session bus
contact. Evidence is the exact commit plus `git diff --check`, repository
source-shape, and the documentation validator; compile/CTest gates are
explicitly requested as reviewer/integrator action on the repair descendant.

## Collision risks

None known. The two parent CMake lines are additive and reviewer-mandated.

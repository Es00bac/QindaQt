# Viewer and common application defaults — claim

- Exact base: `de8786e15e5ad8e8b9b63c49866f4b284f5c7bce`.
- Viewer owner: collaboration worker `/root/viewer`, branch `feat/qindatk-viewer`,
  worktree `.cache/viewer-defaults/viewer`. Owns Viewer source/tests/page.
- Defaults owner: collaboration worker `/root/defaults`, branch
  `feat/common-app-defaults`, worktree `.cache/viewer-defaults/defaults`.
  Owns default-applications route, desktop MIME defaults, session seed removal,
  focused tests and its primary wiki page.
- Manager owns shared documentation/navigation, package dependency integration,
  exact review routing, and combined-tree gates. Workers have disjoint source
  paths; each has a private build root limited to two compiler jobs.
- Decision: ADR-0217; QindaTK UI, Poppler Qt6 renderer, Qt image readers,
  GPL-3.0-or-later, desktop-scoped XDG defaults with user precedence.
- Acceptance: real image/PDF rendering, useful QindaTK UI, malformed input and
  page/zoom checks; settings persistence and inherited-default lookup;
  QindaMPV desktop ID, staged installation and isolated MIME queries;
  independent exact-commit review, affected combined tests, strict docs/links.
- Next gate: immutable tested candidates. No installed adoption claimed.

# Combine with the desktop's committed screensaver work

The user requested that this work also reach qinda and build together with
Opus's screensaver/lock changes. Read-only SSH inspection found the repository
at `~/work_SPaC3/container-wm`, main `072ca5ae4231ff996d4a8e2746cc88b7c0745f24`.
Its parent `2bf5847f` implements the lock-screen saver. Both commits now precede
the manager's local work, fetched directly from that repository.

Remote `Manifest` edits and an untracked `pre20260919-r2` recipe are preserved.
An observed `ninja -v -l24 -j24` compile is not this task's build. The final
combined source will use a separate worktree/build location to avoid collision.
No live session, installation or lock state was changed.

Shared collisions are bounded: defaults removes only the directory-handler
seed, preserving Opus's new Meta shortcut seed; install stanzas are additive.
ADR-0216 belongs to the locker, so the Viewer decision is ADR-0217.

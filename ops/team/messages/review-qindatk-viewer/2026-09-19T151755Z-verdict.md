# REJECT — f56b9a7014849da8403a1f7d8937de91451e0d88

- Candidate: `f56b9a7014849da8403a1f7d8937de91451e0d88`.
- Base: `de8786e15e5ad8e8b9b63c49866f4b284f5c7bce`.
- Verdict: **REJECT**. P0/P1/P2/P3: **0/0/1/0**.
- Product source remains immutable in `.cache/viewer-defaults/review-viewer`.

## Blocking finding

**P2 — encode PDF passwords according to Poppler's public contract.**
`src/apps/viewer/viewer_controller.cpp:81` calls `password.toUtf8()` before
passing the bytes to `Poppler::Document::load`. The installed public
`poppler-qt6.h` explicitly requires Latin-1 bytes and tells callers to use
`QString::toLatin1()` (lines 1029–1031; also `load` parameter docs).
A valid PDF Standard Security revision 2 document with password `café` therefore
cannot be opened through the viewer even when its correct password is entered.

Independent reproduction in ignored review output:

- `.cache/review-extra/make_password_fixture.py` generates the self-authored R2
  PDF `.cache/review-extra/latin1-password.pdf`; its algorithm also checks the
  existing fixture's owner-password value for the known ASCII password.
- `.cache/review-extra/review_password.cpp` first renders this file successfully
  through the candidate `DocumentRenderer` with `QString("café").toLatin1()`.
  The same test calls `ViewerController::unlock` with the same QString and
  fails `controller.ready()` with `The password did not unlock this PDF.`
- Build: `cmake --build .cache/review-extra/build --target viewer_review_password -j2`,
  exit 0. Run under the same private test environment: exit 1, **2 passed,
  1 failed**, zero skips. Full output: `.cache/review-extra/password.log`.

Requested repair: obey Poppler's encoding contract and add a non-ASCII password
regression. The fixture and reproduction were routed directly to the same
implementer and manager. Send one exact repaired commit for this reviewer to
recheck the failure and affected tests.

## Passing evidence

- Fresh Debug configure with shell/production/KWin OFF: exit 0.
- `cmake --build .cache/build-review-viewer --target qindaqt-viewer qindaqt_viewer_renderer_test qindaqt_viewer_controller_test qindaqt_viewer_ui_test -j2`: exit 0.
- `ctest --test-dir .cache/build-review-viewer -R '^apps.viewer\.' --output-on-failure -V`: exit 0, **4/4**, 4.12 seconds; QtTest totals **13/6/3**, zero failures/skips. CLI and relocated install verify desktop metadata, embedded QML, private AppShell dependencies, and real QindaTK UI.
- 960×680 and 640×480 generated layouts visually inspected.
- Review-only wrong-password → post-transition focused retry → cancel → visible Unlock → reopen → successful ASCII password flow: exit 0, **3/3**.
- Exact diff whitespace check: exit 0; review worktree `git status --short` remains empty.

Bounded caveats remain as documented: no physical display/assistive-technology
qualification, first raster frame, and no print/search/forms/annotation scope.
Manager owns shared ADR/nav integration and ADR0218 renumbering. Those pending
shared changes are not candidate defects.

Read the first-party queue after routing the verdict. Bounded help offered:
review the manager's default-ON viewer option and generic Arch CI opt-out diff;
keep the same reviewer available for the exact password repair.

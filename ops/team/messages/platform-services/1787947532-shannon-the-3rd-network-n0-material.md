# Material checkpoint: all eight rejected Network N0 failures reproduced

- Worker: Shannon the 3rd
- Timestamp: 2026-08-28T20:05:32Z
- Exact repair base: `e3e2719dfb3f76b119c4c6c7ccd1193012acff35`
- Worktree: `/mnt/d/QindaQt/worktrees/network-n0-repair-shannon3`
- ADR allocation: manager-reserved ADR-0045

## Executable baseline evidence

- Turing the 3rd's unchanged external adversarial executable reports exactly
  2 passed / 8 failed, exit 8: payload owner, real A/B/A lineage, unbounded
  lease, diagnostic cap, quoted secret, Unicode format control, `wireValid`,
  and sticky failed start all reproduce.
- A fresh strict-warning Debug configuration with KWin and production shell
  disabled built the exact three libraries plus nine focused executables.
  Its registered installed-consumer row fails, exit 8 from CTest, because the
  whole-tree install requests the unbuilt unrelated `libqindaqt_profiles.a`.

## Source repair now present but not yet qualified

- The client authenticates decoded `Snapshot::owner` against the exact request
  owner and rolls every live/public flag back when transport start fails.
- The model retains a per-instance lineage high-water across clear/loss while
  published snapshot and lease truth clear; a real A to B to retired A path is
  now explicitly tested.
- Scan leases carry a bounded 1--120 second duration on wire and convert to an
  overflow-checked local monotonic deadline only during atomic model adoption.
- Shared Unicode scalar policy rejects control/format/surrogate/private-use/
  unassigned presentation text while retaining valid supplementary Unicode.
  Validation rejects unredacted secret-shaped public diagnostics and false
  wire-valid flags; codec booleans accept only bytes 0 or 1.
- The redactor handles single/double-quoted, unquoted, and malformed quoted
  credential assignments, sanitizes dangerous scalars, and budgets the UTF-8
  ellipsis inside the exact 512-byte cap. Retry schedules are count-bounded.

## Remaining gate

Compile and repair any strict-warning defects, add the registered consolidated
hostile row plus source-policy poison negatives, isolate all three Network
installs in one component for Debug and Release, add ADR-0045/module/testing
documentation and accurate privacy/frequency wording, remove candidate-local
activity files, then run the full exact evidence matrix. Turing the 3rd remains
the required rereviewer; this checkpoint is not a candidate handoff.

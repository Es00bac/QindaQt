# App appearance candidate review — 2026-09-06T02:46:22Z

- Candidate: `4f166d31b6c1c15ef2b9646c30b373e05bc4d460` in `.cache/app-appearance`, base `2981a286`.
- Status: blocking review finding.
- Evidence: `ApplicationAppearanceController::applySnapshot()` reads `QGuiApplication::styleHints()->colorScheme()` only when the SettingsClient emits `snapshotChanged` (`src/app_appearance/src/application_appearance_controller.cpp:111-146`). The controller never connects to `QStyleHints::colorSchemeChanged`. Therefore a confirmed `appearance.colorScheme=system` process does not re-resolve or emit `appearanceChanged` when the platform changes Light/Dark; existing Editor/Terminal native palettes and File Manager QML tokens remain stale until an unrelated Settings snapshot. Add a GUI-thread style-hints connection (guarded to avoid duplicate/null setup) and a focused regression that changes the platform scheme or exercises the callback and verifies the selected compatible theme/update.
- The resolver policy and consumers otherwise preserve explicit CLI locking, high-contrast compatibility, built-in light/dark fallback, scoped two-key Settings reads, and native widget propagation (Editor views and Terminal children). Existing candidate tests cover only pure resolver behavior; they do not prove controller snapshot/lifetime/native reload or platform-scheme callback behavior, so those tests should accompany the repair.
- Integration note: candidate adds `docs/wiki/adr/0079...`; the Welcome-side work also owns ADR-0079. Rename/supersede one ADR during integration to avoid duplicate-path collision while preserving reciprocal index links.

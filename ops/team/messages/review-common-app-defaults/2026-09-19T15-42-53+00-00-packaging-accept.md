# ACCEPT — QindaMPV and desktop-r4 packaging metadata/archive review

- Scope: manager-requested bounded recipe, dependency, source archive, and Manifest review. No product re-review, build, install, global overlay edits, or remote operations.
- Packaging findings: P0 0 / P1 0 / P2 0 / P3 0.
- Requested next action: retain these exact recipe/archive bytes in the integration checkpoint and continue the manager's combined package/build/deployment gates.

## Exact reviewed recipes

| File | SHA-256 |
| --- | --- |
| `packaging/gentoo/media-video/qqmpv/qqmpv-0.1.0_pre20260919.ebuild` | `bba71e6166d7057c60d7d4e1ea8cfeee3f8549aba48822100cbd376ebb211fd5` |
| `packaging/gentoo/media-video/qqmpv/Manifest` | `e9f34df53c0a163b85f63e28fc92bc9840bdcb85937f2a59ec251d3b13779002` |
| `packaging/gentoo/gui-wm/qindaqt-desktop/qindaqt-desktop-0.1.0_pre20260919-r4.ebuild` | `b3bae51e9dccc545bb2e8afa685ea14ec25216e40747922a5c5008049266d829` |
| `packaging/gentoo/gui-wm/qindaqt-desktop/Manifest` | `04d6c899e2ca85576035b4a87b085e9ef9b021191d495e5524dc1430c2d2fd26` |

The companion recipe is committed in source boundary `18d766fa0c4aeb345bed929aef9ae72ffd4e1b11`; the new desktop-r4 recipe points to that exact integrated source. The desktop revision avoids the externally reserved r3.

## Archive checks

The QQMpv archive is 23,570 bytes. Both BLAKE2B and SHA-512 match its Manifest. All 35 archive members have the exact `QindaMPV-6bfde6644d927989fcd157471873543c75b5b05b/` prefix; the Git pax comment matches the pinned commit. There are no absolute/traversing member names or archive links. The CMake install rules place `qqmpv`, `org.qindaqt.QQMpv.desktop`, and its icon in the standard binary/application/icon directories. The desktop entry advertises the same eight media MIME types used by the packaged defaults.

The desktop-r4 archive is 24,190,152 bytes. Both Manifest digests match, gzip mtime is zero, all 7,173 entries have the expected prefix, and the Git pax comment names `18d766fa0c4aeb345bed929aef9ae72ffd4e1b11`. Its entire decompressed tar stream was compared against a fresh `git archive --format=tar --prefix=QindaQt-18d766fa0c4aeb345bed929aef9ae72ffd4e1b11/ 18d766fa0c4aeb345bed929aef9ae72ffd4e1b11`; both SHA-256 values are `cccddc2a4b16e81b3b2521f0e2ed9a7e79c9d37fab12e822ed4a3a14daacde57`. All 55 previous desktop Manifest rows are unchanged; exactly one distfile row was added.

## Dependencies and metadata

The companion source's required Qt Core/Gui/QML/Quick/QuickControls2/DBus/OpenGL modules, private Qt headers, QindaTK QML import, runtime libmpv, and installed AppShell/menu libraries are represented by the recipe's declared packages and the inherited CMake eclass dependencies. The installed AppShell library/header destinations match the source's lookup rules. Current local Gentoo ebuilds confirm the specified Qt/mpv/Poppler USE flags exist; QindaTK introduces no dependency back to the desktop.

Compared with desktop-r2, desktop-r4 changes only the exact source pin, adds QindaTK/Poppler Qt6/image-format dependencies, explicitly enables Viewer, and adds the player as PDEPEND separately from DEPEND. The fetch-restricted filename-only companion SRC_URI is permitted by [PMS8 section 7.3.2](https://projects.gentoo.org/pms/8/pms.html). The desktop's PDEPEND placement allows its installed AppShell to satisfy the player's build dependency; [PMS8 dependency classes](https://projects.gentoo.org/pms/8/pms.html) require post dependencies by the end of the install batch.

Both recipes pass `bash -n` (exit 0). Eight explicit actual/planned dependency atoms also parsed under Portage EAPI 8.

Actual Portage metadata evaluation passed **2/2** using the installed Portage `portdb.aux_get()` path, the real inherited Gentoo eclasses, copied recipes in a private review repository, private configuration/tmp/logs, and a newly created verified private dependency cache. Evaluated metadata confirms EAPI 8, both source URIs, fetch restriction, `cmake xdg` inheritance, inherited Ninja/CMake BDEPEND, all expanded package dependencies, and the exact desktop PDEPEND with no player build dependency.

Reproduce from the manager checkout:

```sh
python3 .cache/viewer-defaults/review-defaults/build/defaults-review/portage-metadata-review/check_metadata.py
```

Result: exit 0, `PASS: 2/2 Portage metadata evaluations`. The script asserts the copied recipes equal the reviewed manager files and that the actual Portage cache is its newly created private directory. Full expanded output is retained in adjacent `evaluated-metadata.json`.

The initial direct `ebuild ... depend` command failed because this installed CLI calls the current Portage API without its required returnproc/returnpid option. An earlier private-cache attempt also encountered the sandbox's unmapped Portage group, resolved using documented `PORTAGE_USERNAME`/`PORTAGE_GRPNAME` set to the current owner. These environment/CLI failures are preserved in adjacent logs and were superseded by the successful fresh-cache metadata gate; neither failure required a global configuration change.

## Limits and follow-up

No package install, dependency solver transaction, companion build, remote process, or deployed media launch was performed by this review lane. Those remain the manager's integration evidence. The companion archive's hashes and embedded pin were verified locally; the independent remote Git-tree verification remains the manager's previously reported evidence. Bounded help offered: inspect an exact package metadata/archive regression encountered during integration. No unrelated backlog was claimed.

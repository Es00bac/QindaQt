# Material finding under reproduction

- Candidate: `6967d76cd792379094a3fd3d3a0342cf7cc06730`
- Source inspection: new `MimeAppsDefaultApplicationsStore::load()` trusts the public scan's visible-only entries to decide whether a configured MIME handler is installed. The shared launcher parser treats `NoDisplay=true` and `Hidden=true` alike, but the [Desktop Entry specification](https://specifications.freedesktop.org/desktop-entry/latest/recognized-keys.html) preserves MIME-handler use for NoDisplay entries.
- Expected impact: Settings can display a lower-priority packaged handler while actual default resolution retains the configured NoDisplay handler. A private C++/xdg-mime probe is prepared under the ignored build directory and will run after the two-job target build finishes.
- Verification so far: independent Debug configure exit 0; `tools/validate-docs` exit 0, 332 documents; strict MkDocs exit 0; `git diff --check` exit 0.
- Requested next action: manager informed through collaboration; await exact probe result before verdict. No source edits, host preference writes, or remote work.

# ACCEPT — 812b6cf73d4c8c55a5242e98cb28cb0a532f3b32

- Exact candidate: `812b6cf73d4c8c55a5242e98cb28cb0a532f3b32`
- Resolves rejected ancestor: `6967d76cd792379094a3fd3d3a0342cf7cc06730`
- Review worktree: `.cache/viewer-defaults/review-defaults`, detached at the exact repaired product commit.
- Findings remaining: P0 0 / P1 0 / P2 0 / P3 1 (unchanged, nonblocking partial-category consistency noted in the first verdict).
- Requested next action: integrate the accepted exact candidate, complete manager-owned ADR/module-boundary documentation, and rerun affected combined-tree/installed-route gates.

## Repair and compatibility evidence

The original P2 reproduction now returns `settingsBrowser=nodisplay.desktop` and `xdgBrowser=nodisplay.desktop`. The private probe is unchanged except for explicitly using `ApplicationVisibility::IncludeNoDisplay`, matching the repaired production Settings composition. `review_probe_repaired.cpp`, its binary, and `probe-repair-review.json` remain under ignored `build/defaults-review/`. The probe uses temporary XDG files only and launches no fixture application.

Source inspection and independent tests confirm that the original public `ApplicationCatalog::build(documents)` and `scanApplicationDirectories(roots,error)` overloads still select MenuEntries. Existing aggregate `ParsedDesktopEntry::hidden` behavior is preserved; the appended `deleted` flag records exact Hidden state. Opt-in MIME discovery retains NoDisplay handlers while Hidden/deleted and malformed higher-root documents still claim their identity before lower-root copies can be considered. The real directory scan test verifies the retained user document/path as well as default menu filtering.

## Independent commands and results

The existing private Debug configuration still has shell, production shell, and KWin plugin disabled. The repaired tree was rebuilt with two jobs:

```sh
cmake --build build/defaults-review --target \
  qindaqt_default_applications_store_tests \
  qindaqt_default_applications_catalog_tests \
  qindaqt_default_applications_settings_model_tests \
  qindaqt_default_applications_page_tests tst_sessiondefaults qindaqt-wm \
  qindaqt_desktop_entry_parser_tests qindaqt_application_catalog_tests \
  qindaqt_launcher_category_model_tests qindaqt_launcher_search_ranker_tests \
  qindaqt_launcher_pinned_recent_tests qindaqt_launcher_presentation_tests \
  qindaqt_application_directory_scan_tests \
  qindaqt_application_catalog_category_tree_tests \
  qindaqt_application_launch_support_tests \
  qindaqt_launcher_scanner_tests qindaqt_launcher_executor_tests -j2
```

Result: exit 0. Log: `build/defaults-review/build-repair-review.log`.

```sh
ctest --test-dir build/defaults-review -R '^(qindaqt.launcher-(desktop-entry-parser|application-catalog|category-model|search-ranker|pinned-recent|presentation|runtime-boundary|contract-text|application-scanner|executor)|application-catalog\.(category-tree|directory-scan|launch-support)|qindaqt.settings-default-apps-(store|catalog|model|page|boundary|boundary-poison|packaged-defaults)|session.sessiondefaults)$' --output-on-failure -V
```

Result: exit 0, **21/21 gates**. There were 195 QtTest passes and zero failures/skips; the staged packaged policy additionally passed 6 cases covering all 18 core MIME types. Direct Qt totals: parser 43; L0 catalog 14; category 7; search 11; pinned/recent 11; presentation 12; runtime scanner 14; executor 12; public category tree 5; directory scan 6; launch support 6; session 13; defaults store 23; defaults catalog 6; defaults model 8; defaults page 4. Log: `build/defaults-review/ctest-repair-review.log`.

- Private repaired probe compiled against the freshly rebuilt archives and ran with equality assertions: exit 0.
- `tools/validate-docs`: exit 0, 332 documents.
- `/home/cabewse/.local/bin/mkdocs build --strict -d build/defaults-review/site-repair-review`: exit 0.
- `git diff --check`: exit 0. Product sources remained unchanged; only self-owned review records/messages are untracked.

## Bounds and bounded help

The pre-existing partial-category choice inconsistency remains nonblocking and was not expanded into this repair. Full Settings installed-route, integration with Opus's work, and deployed application launch remain manager gates. No remote work or host preferences were changed. The First-party queue and current implementer repair thread have been read; bounded help offered is reproduction of an exact combined-tree defaults/catalog regression, with no unrelated backlog claim.

# Viewer verification

The complete assigned viewer outcome is implemented and ready for immutable
candidate review. `qindaqt-viewer` uses QindaTK presentation, existing Poppler
Qt6 PDF rendering and Qt image decoders. It opens local files/file URLs and a
single drop, renders every PDF page, supports page jumps, fit/zoom, rotation,
passwords, error/empty states and AppShell standard menus.

Evidence in the private `.cache/build-viewer` tree:

- Configure with shell/production/KWin disabled and target-only `-j2` build:
  exit 0. Final incremental app rebuild: 7 steps, exit 0.
- `ctest --test-dir .cache/build-viewer -R '^apps.viewer\.' --output-on-failure`:
  exit 0, **4/4** in 4.27 seconds.
- Real renderer **13/13**, controller **6/6**, offscreen UI **3/3** QtTest rows,
  all zero failure/skip; CLI/staged smoke verifies help, positional cardinality,
  paths with Unicode/spaces, file URLs, remote rejection, desktop metadata,
  relative shared-library resolution and embedded installed QML.
- All eight advertised MIME types were decoded. PNG metadata containing a PDF
  marker is still an image. PDF fixture has two independently colored pages;
  password fixture rejects a wrong password and accepts `viewer-test`.
- Visually inspected `tests/apps/viewer/artifacts/viewer-960x680.png` and
  `viewer-640x480.png` inside the build tree; controls and document are visible.
- Scoped source shape with warnings-as-errors: production **11 files**, tests
  **6 files**, exit 0. `desktop-file-validate` and `git diff --check`: exit 0.

Bounded caveats: first image frame only; no print/search/annotation/forms or
continuous PDF layout. QindaTK uses its existing theme. No physical-display,
screen-reader, or new nested compositor qualification is claimed. The complete
tree source-shape gate has known pre-existing failures outside this ownership.
`tools/validate-docs` and absolute `/home/cabewse/.local/bin/mkdocs build --strict`
each exit 1 only for the pending manager-owned ADR-0217 link; the manager owns
nav/ADR integration and the final full docs gates.

Requested next action: a different worker reviews the exact forthcoming
candidate commit, then the manager integrates and reruns the four viewer gates
with default-app/packaging changes. Available bounded help: repair exact viewer
findings in this worktree or verify the integrated staged Viewer component.

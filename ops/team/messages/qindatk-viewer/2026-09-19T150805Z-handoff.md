# Exact viewer candidate handoff

- Exact product commit: `f56b9a7014849da8403a1f7d8937de91451e0d88`
- Base: `de8786e15e5ad8e8b9b63c49866f4b284f5c7bce`
- Branch/worktree: `feat/qindatk-viewer`, `.cache/viewer-defaults/viewer`
- Changed paths: `src/apps/viewer/**`, `tests/apps/viewer/**`,
  `docs/wiki/apps/viewer.md`, this worker/thread; additive viewer registrations
  in `src/CMakeLists.txt` and `tests/CMakeLists.txt`; explicitly authorized
  `COMPONENT Viewer` backing-library entries in `src/app_shell/CMakeLists.txt`,
  `src/controls/CMakeLists.txt` and `src/design_tokens/CMakeLists.txt`.
- Acceptance evidence: target build exit 0; focused CTest **4/4** exit 0;
  renderer **13/13**, controller **6/6**, UI **3/3** plus CLI/staged install;
  scoped source shape **17 files** exit 0, desktop metadata and staged diff
  checks exit 0. Full commands and bounded caveats are in
  `2026-09-19T150448Z-verification.md`.
- Requested next action: independent exact-commit review, then manager
  integration with ADR-0217/defaults/package work and integrated focused/docs
  gates. No integrated progress or independent acceptance is claimed yet.

After handoff, read the current First-party queue and defaults peer's handoff
and NoDisplay repair claim. Concrete help offer: reproduce and repair exact
viewer review findings in this same worktree, or verify the integrated staged
Viewer component. The defaults parser repair remains owned by that worker.

This reply and the corresponding live-board update are coordination only;
the product candidate above remains immutable.

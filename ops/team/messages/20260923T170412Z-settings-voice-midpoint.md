# A01 Voice midpoint

The new VoicePreferences gate admits desktop Voice use only from a current-owner confirmed On baseline. Shell, Settings and console use it; default Off/missing Settings1 baseline cannot activate Voice1. The Settings route stays editable while Off and applies an explicit Off locally before the asynchronous write. Console and route Try again controls cannot bypass the gate. A sequential two-key draft waits for the first key's fresh snapshot before submitting the second key.

Focused Voice production targets compile, the qindaqt.settings-voice CTest row passes, and strict MkDocs passes. The worktree-local qindaqt-shell target is still compiling with four jobs. No candidate is handed off yet.

# Olive Hazlett-Codex — Display Color C1 second repair midpoint

- Timestamp: 2026-09-03T06:33:51-06:00
- Candidate under repair: `57bcd04e32b8e0e42e3eeeece9eae84db0d2e1dd`

Material finding: preserving the injected path spelling for diagnostics is
compatible with canonical containment because root admission first proves that
its lexical absolute and canonical paths are equal. The implementation now
retains one canonical root plus one open directory descriptor for import,
rejects `..` before normalization, canonicalizes every discovery candidate and
import destination (or its existing parent), and returns before a private
recording destination-inspector seam when the root is invalid.

Focused strict compilation is green. The isolated full selector passes 16/16
in Debug and 16/16 in Release, including all package rows. `validate-docs`,
strict MkDocs, source shape, and `git diff --check` also pass; final-byte reruns
remain before the candidate commit.

# Contrast candidate review — f7ea8f6d403d6fd52b538a3eb2d518a84f033daf

- Status: BLOCKED by two remaining concrete contrast paths.
- Candidate fixes the audited warning-foreground misuse list and status attention glyph, and changes disabled Button rendering to muted-on-raised. However, `src/apps/settings/appearance/qml/AppearanceDesktopSection.qml` remains unchanged: bundled card custom `Label` uses `Tokens.fg.default` even when selected card background is `Tokens.accent.default`; selected wallpaper labels therefore need `Tokens.accent.fg` (or a selected-aware role).
- `src/shell/icons/qml/Icon.qml` appends any `root.color` with `a > 0`, while the provider accepts only opaque `#rrggbb`. Passing `Tokens.fg.disabled` (alpha overlay) therefore sends a rejected 8-digit color and leaves the native symbolic SVG's authored ink, commonly dark/black. Remaining consumers at ordinary panel surfaces: `src/shell/desktop_controls/qml/QuickLaunchApplet.qml:52-59`, `ApplicationTilesApplet.qml:36-43`, and the SystemStatus placeholder at `SystemStatusApplet.qml:81-87`. The lane icon was changed to muted, but placeholder paths still pass disabled. Use opaque `Tokens.fg.muted` for these essential unavailable glyphs or add a documented opaque disabled icon role; do not rely on provider alpha recolor.
- Theme palette changes and updated built-in contrast tests are otherwise coherent; warning messages now use `fg.default` on plain surfaces, and disabled Button pairs use muted/raised. Re-run icon/QML contrast checks after the two remaining fixes.

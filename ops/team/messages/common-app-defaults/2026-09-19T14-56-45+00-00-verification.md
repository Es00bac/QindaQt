# Verification — common application defaults

- Time: 2026-09-19T14:56:45+00:00
- Outcome: packaged common-app defaults and independent PDF category; inherited installed handlers; selected-category persistence without stale preference rewrites.
- Focused CMake configure and build: exit 0, shell/production/KWin OFF, -j2. Existing unrelated Qt PrintSupport CMake warnings remain.
- CTest: 8/8 passed, exit 0. Session QtTest 13, store 22, catalog 6, model 8, page 4 passes (including lifecycle rows); two boundary checks; staged-install xdg-mime suite 6 cases.
- Source/scanner fixture revealed and repaired prior suffix mismatch: ApplicationCatalog IDs omit `.desktop`, MIME IDs require it. Production lookup and candidate projection now convert explicitly; real scanned entry round-trip proves it.
- Staged policy proof executes actual session-local CMake install rules, using CMAKE_INSTALL_LOCAL_ONLY to avoid unrelated unbuilt desktop-controls artifacts; then 18 core MIME types and optional browser/mail, unavailable-handler fallback, user/admin precedence and desktop scoping are queried in isolated roots.
- `tools/validate-docs`: exit 0, 332 documents. `/home/cabewse/.local/bin/mkdocs build --strict`: exit 0. `git diff --check`: exit 0.
- Bounded nonclaims: full Settings installed-route relocation and live app launch require manager combined-tree build/deployment; no host preferences or sibling repository changed.
- Next action: independent exact-commit review, then manager integration preserving the remote lockscreen seed changes.

# Material finding and implementation

- Time: 2026-09-19T14:50:02+00:00
- Existing Settings store rewrote all categories and ignored inherited defaults; login seeded a user directory-handler preference above administrator/package policy.
- Replaced those behaviors with selected-category writes and XDG ordered default lookup. Desktop-specific user overrides remain writable in their owning file; unrelated defaults and Added/Removed Associations survive.
- Packaged MIME policy uses QindaMPV's exact eight advertised types and optional Firefox/Thunderbird IDs. PDF gets an independent choice and image coverage includes BMP/TIFF/SVG.
- Private CMake configure passed with shell, production shell, and KWin plugin off; target-only build is running with -j2.
- Next gate: C++ store/catalog/model/page and session tests plus staged-install xdg-mime tests entirely inside temporary XDG roots.

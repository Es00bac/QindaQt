# First-party menu polish second review repair

The second review identified two remaining endpoint-lineage gaps. Native
announced-service owner loss now withdraws the exact bound endpoint before
focus refresh clears transport state. Each dbusmenu client unavailable callback
captures its client generation and endpoint; a callback queued by a retired
client cannot withdraw or clear a replacement binding.

Failed authentication preserves a valid old acknowledgment when focus moves to
a different bad application. It withdraws the attempted endpoint, plus a prior
endpoint only when the same window changed endpoints, matching the no-height-
jitter contract without retaining stale same-window truth.

The transport test queues an old-client failure immediately before synchronous
endpoint replacement and proves the replacement stays available. Its native
announcement row now proves owner loss emits the exact `hosted=false` endpoint.
The four focused AppShell/global-menu rows pass 4/4; diff, source-shape, and
documentation validation pass.

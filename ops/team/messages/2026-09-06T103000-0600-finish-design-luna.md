# Follow-up scope and render note

The previous `build/design-studies/renders/qinda-punk-production-*.png` files are Qt `qindaqt-shell-preview` developer profile demos, not production Settings or nested-session evidence. They are useful only for palette inspection. Root's final acceptance should use the installed nested Settings session with `data/wallpapers/qinda-punk.png`.

This follow-up adds tokenized `QindaQt.Controls.ComboBox`, changes SegmentedChoiceRow so only its checked choice uses accent fill, and changes the qindaqt profile default wallpaper to `qindaqt:qinda-punk`. Existing shell preference tests still encode the old jade-fold default; shell owner should update those expectations in the integrated tree while preserving explicit and empty wallpaper choices.

# Icon candidate review — d8c246be53e741756320dbb8d10be7c8cba70b08

- Status: BLOCKED only on documentation completeness; implementation/coverage checks pass.
- `python3 tools/validate_qinda_icon_theme.py` passes: 286 SVGs, 131 canonical icons, 12 aliases, 7 groups. It now parses all literal names from `tests/shell/verify_shell_icon_coverage.cmake`, catches stray XML text/tails, and renders WiFi symbolic levels with `rsvg-convert` + Pillow alpha-mask checks. The exact missing coverage names are present, and the WiFi helper wraps every arc in `stroke()`.
- `git diff --check 8b118917..d8c246be` is clean; no validation PNGs remain. Index/theme, no hicolor replacement, SVG viewBoxes, transparency, and alias contracts are sound on inspection.
- Documentation gap: `docs/wiki/shell/icon-theme.md` tells users to run `python3 tools/validate_qinda_icon_theme.py` but does not state that this validator requires Pillow and `rsvg-convert` (nor mention the required XML tooling for the following command). Add the dependency note before acceptance so the documented validation workflow is executable.

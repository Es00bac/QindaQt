# File-manager ownership notice

The user reports a separate Kimi K3 Max worker working on the file manager.
This team reserves the file-manager implementation, focused tests, and primary
wiki pages for that worker. The user subsequently clarified that this includes
SMB/network-filesystem browsing and filesystem mounting integrations. No branch, worktree, provider execution, or candidate
has been independently inspected, so this is an ownership notice rather than a
liveness or completion claim. Coordinate shared build or public-interface
changes before editing across that boundary. Existing desktop checkpoint work
continues in Settings, shell/compositor, global menus, and input compatibility.

# Artwork visual verification plan

Prepared ignored `.cache/artwork-visual/plan.md`; no capture was run. Proposed bounded views: real wallpaper baseline; launcher popup with Pin/Unpin and resolved icons; populated dock with 40px artwork/hover tooltip; real File Manager folder contents and icons; Text Editor global menu without local duplicate; restored Settings/Editor final frame. Uses only nested `InjectTestInput` and Weston screenshots, preserves the initial Settings/Editor window ledger, requires zero private-process survivors, and records stage/build id plus event logs. File Manager coverage is conditional on the integrated fixture exposing `org.qindaqt.FileManager`; no mock substitution. Current runtime stores wallpaper preference only, so wallpaper success requires the new art consumer rather than existing settings storage.

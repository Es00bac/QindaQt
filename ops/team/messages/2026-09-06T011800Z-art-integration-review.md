# Art integration review — 9ed8395fe48ef2e7cdbbad0b6981a60d027c05e1

- Status: BLOCKED — exact candidate is internally inconsistent and cannot compile as committed.
- Evidence: `AppearanceSettingsModel` in the candidate requires `(SettingsClient&, QVector<ThemeSpec>, QVariantList bundledWallpapers, Qt::ColorScheme, TokenFacade*, QObject*)` (`src/apps/settings/appearance/include/qindaqt/apps/settings_appearance/appearance_settings_model.h`), but the candidate's `src/apps/settings_center/main.cpp:165` still constructs it with `(appearanceClient, *themes, application.styleHints()->colorScheme(), facade)`. The candidate also does not include `wallpaper_catalog.h`, `wallpaperSearchDirectories()`, or a `discoverBundledWallpapers(...)` argument in `main.cpp`. This is a compile-time arity/type failure, independent of runtime tests.
- The current worktree has an uncommitted main.cpp repair that adds the missing include/search/argument, but it is outside exact commit `9ed8395f` and therefore cannot be used as acceptance evidence. The repair must be committed in the replacement candidate and re-reviewed.
- Installed artwork layout itself is coherent in the exact tree: `src/apps/settings/appearance/CMakeLists.txt` installs `data/wallpapers/` to `${CMAKE_INSTALL_DATADIR}/qindaqt/wallpapers` and `data/icons/QindaQt` to `${CMAKE_INSTALL_DATADIR}/icons`, both under `SettingsAppearanceRuntime`; three PNGs are present (`ink-tide`, `jade-fold`, `porcelain-dawn`). Shell icon roots already derive from freedesktop generic data roots, so `${prefix}/share/icons` is searched when that install component is present.
- `wallpaper_catalog.cpp` is PNG-only, ordered per root, case-fold deduplicated, and emits absolute paths/labels; docs correctly state storage-only behavior. Those points remain contingent on wiring the catalog into `main.cpp`.
- Requested action: add the missing executable wiring to the replacement candidate, then provide exact hash and compile/focused test evidence. Do not claim acceptance for 9ed8395f.

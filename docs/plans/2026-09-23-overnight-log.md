# Overnight log: Settings / QindaTK / Network plan

Plan: [2026-09-23-settings-qindatk-and-network-plan.md](2026-09-23-settings-qindatk-and-network-plan.md)

## Staffing rules for tonight (from Jarrod, 2026-09-23 ~23:20 MDT)

- The five Opus workers started at ~23:05 finish their current tasks; they were not stopped.
- After that: at most **two** Opus agents at a time. Reviews and follow-ups go to **GPT-6 Luna at max
  reasoning** (`codex exec`, run detached on qinda), one or two at a time. No large parallel runs.
- Spoken updates continue overnight as usual (Jarrod asked for this); this file is the written record.
- The laptop is kept awake until ~11:20 MDT tomorrow by a `systemd-inhibit` lock ("Claude Code").
  Remove it early with `pkill -f "Overnight orchestration"`.
- The workstation (qinda) is kept awake the same way until ~11:35 MDT by a temporary system service,
  `claude-overnight-inhibit`. Stop it early with `sudo systemctl stop claude-overnight-inhibit`.

## Timeline

- **~23:05** Started five Opus workers on qinda, one per workstream: W0 Wi-Fi agent fix, W1 Settings
  search, W2+W3 bars and empty states, W4 QindaTK r5 + KeyCap, W5 Network applet. Each has its own
  worktree under `~/work_SPaC3/container-wm/.cache/claude-plan-20260923/`, based on `2e415cad`.
- **~23:10** Baseline strict MkDocs build of `main` passes on the laptop.
- **~23:20** GPT-6 Luna launcher verified on qinda (smoke test returned OK).
- **23:29** Jarrod reported Screen saver → Preview brings up the lock screen. Cause: ADR-0226 clause 4 previews through `kscreenlocker_greet --testing`. Added as **W6** to the plan; queued for the next free worker (Luna).
- **23:33** **W4 candidate ready.** QindaGentoo `b44fd699` (adds `qindatk-0.1.0-r5` pinning QindaTK `89d5ca39`; no dependency changes; QindaTK QML suite 120/120 at that commit) and container-wm `e41a4817` (Input shortcuts drawn with `Tk.KeyCap`, one group per binding; Input tests 12/12 against the r5 toolkit; against the installed r4 they fail with "KeyCap is not a type", as expected). **Release note:** the desktop ebuild must require `>=dev-libs/qindatk-0.1.0-r5` in the same release.
- **23:33** Started two GPT-6 Luna (max) runs on qinda: reviewer `luna-review-w4` and worker `luna-w6` (screensaver preview). Four Opus workers still running (W0, W1, W2+W3, W5).
- **23:42** **W0 candidate ready**: `548be376` (secret agent admits the D-Bus shapes NetworkManager really sends, incl. DNS and wired 802.1X forms the snapshot missed; agent stays resident; 39/39 network tests; mutation checks confirm the new tests catch both bugs). Strict MkDocs passes on the laptop. Review (`luna-review-w0`) queued for the next free Luna slot.
- **00:04** **W4 review (Luna):** QindaGentoo r5 ebuild `b44fd699` **ACCEPT** (independent build 120/120; no dependency change). KeyCap change `e41a4817` **REJECT, packaging only**: the code and tests are fine (12/12 with r5), but the desktop ebuild still allows QindaTK r4, where the Input page fails with "KeyCap is not a type". Resolution: KeyCap integrates only in the release whose desktop ebuild requires `>=dev-libs/qindatk-0.1.0-r5` (r6), never in the W0-only r5.
- **00:04** Started `luna-review-w0` (Wi-Fi agent, security-focused).
- **00:11** **QindaTK r5 released.** Overlay hub `master` fast-forwarded to `b44fd69` (accepted r5 ebuild). Installed `dev-libs/qindatk-0.1.0-r5` through Portage on **both** machines; the installed toolkit now provides `KeyCap`. Snag found and fixed: the laptop's own bare mirror `~/git/QindaTK.git` (which Portage reads first) was stuck at the r4 commit; fast-forwarded it from qinda's hub. Added that mirror detail to the Claude/Codex source-of-truth rules on both machines.
- **00:31** **W6 candidate ready** (Luna worker): `b443f394` (impl `a5300bbb`). Preview runs each saver with the catalog's program and arguments; Blank is a Settings-owned black window that closes on any input; the greeter path is removed; ADR-0259 supersedes ADR-0226 clause 4. Screensaver tests 6/6. The worker's broad Settings run was not meaningful (focused build only), so the reviewer is required to build and run the full Settings suite. Strict MkDocs passes on the laptop. Started `luna-review-w6`.
- **00:37** **W0 review (Luna): REJECT** — two secret-wiping gaps: (B1) measuring UTF-8 size of `a{ss}` values (NetworkManager's VPN-secrets shape) creates temporary copies that are freed unwiped; (B2) decoded `aay` / IPv6 address byte arrays are released unwiped. Everything else checked out (cursor handling, limits, fail-closed, lifetime test, no logging; 39/39 tests; 15/15 repeated runs; reviewer's own oversized-map attack refused). Repair assigned to Luna (`luna-w0-repair`) in the same worktree, as a new commit; re-review follows.
- **01:09** **W0 repair done** (`luna-w0-repair`): product commit `b6fb166b`, branch head `5ac3325e`. Allocation-free UTF-8 sizing; scope-guard wipes for decoded `aay`, IPv6, `a{ss}`, `a{sv}` data and map keys; new regression tests (VPN-shaped `a{ss}` admit/refuse, `aay`/IPv6 limits, wipe helpers). 39/39 network tests, 15/15 repeats; strict MkDocs passes on the laptop. Re-review started (`luna-review-w0-recheck`).
- **01:42** **W0 recheck (Luna): REJECT on a new point, B3.** B1/B2 confirmed fixed (reviewer's own exhaustive UTF-8 check: all 65,536 code units + 1,048,576 surrogate pairs). B3: the wipe helpers zero shared (implicitly shared) memory, so a live alias is zeroed too. **Manager ruling:** that write-through is main's pre-existing, deliberate secret-scrubbing contract (existing AGENT-GUARD), and every new wipe site only touches freshly decoded locals, so it is not a defect in this candidate. It is closed with a follow-up (`luna-w0-repair2`): make the contract explicit in code, and add a test proving the stored request is byte-for-byte intact after admission. The same reviewer rechecks afterwards.
- **~01:45–03:40** Claude's usage limit ran out. The three Opus workers (W1, W2+W3, W5) were cut off mid-task; their worktrees kept all work (W2+W3 committed `052dbf7b`; W5 committed `4f1d214a`; W1 uncommitted).
- **During the pause** Luna finished: **W0 follow-up** (`35af19a6`, product `58e71c99`): wipe contract documented; the new regression found and fixed real alias cases in prompt/reply metadata; 39/39, 15/15. **W6 review: REJECT**, 4 valid findings: Blank preview could stay stuck if input never arrives; partial multi-display show counted as success; saver crash/nonzero exit not reported; and the normal idle lock could still fire over a long preview (Jarrod's core requirement). Full Settings suite on W6 was 133/133.
- **03:42** Limit reset. Started Luna `luna-review-w0-recheck2` and `luna-w6-repair` (all four findings, including idle inhibition during Preview). Resumed two Opus workers from where they stopped: W1 (search) and W5 (network applet). W2+W3 is queued for the next free slot.
- **03:50** **W1 candidate ready**: `e59d4787` (Ctrl+K search: keywords for all 21 routes, Input sub-pages, Escape guard, compact layout; full Settings suite 135/135; ADR-0257). Notes for review: `Main.qml` grew from 377 to 384 non-blank lines (source-shape limit 350 was already exceeded at base); a 10-line InputPage change for repeated destinations. Strict MkDocs passes on the laptop. Review staged for the next free Luna slot. Resumed the W2+W3 Opus worker to finish and hand off (Opus now: W5 + W2/W3).
- **03:54** **W2+W3 candidate ready**: `7c358192` (code `052dbf7b`). Signal and charge meters (no bar when charge unknown; warning/danger colours at severity 3/4+); EmptyState on 6 pages (skipped: InputPointerSection because an open Input branch edits it; LoginScreenPage because it is already over the QML size limit; ClipboardPage because those aren't empty-list labels). Full Settings suite 134/134; strict MkDocs passes. **Claude's own look at the screenshots:** battery bars don't line up (each starts at a different place and has a different width), and Wi-Fi bars shift when a Connect button is present. Flagged to the reviewer as blocking. Review staged.
- **04:09** **W0 ACCEPTED** on the second recheck (`35af19a6`): reviewer traced every path; nothing still in use gets wiped. Started `luna-review-w1` in the freed slot.
- **04:09** **W0 integrated on qinda main**: merge `f02c5a0b` + review records `c04baab9`. Integrated-tree gate: secret-agent targets rebuilt; secret-agent tests pass (8 rows). Full build + network suite + validate-docs running in the background before release.
- **04:09** **W5 candidate ready** (Opus): `fef2c406`. Network applet: status icon, Wi-Fi/mobile switches, connections with Disconnect, visible networks (saved/secured written out), connect known/new, Rescan, Network Settings…; success only after Network1 confirms; no password path; System Status network lane. Placement: own icon in the default layout; in the GNOME/macOS/Unity-style layouts it lives in System Status (those layouts don't show Bluetooth/Power icons separately either) — accepted. Focused tests 26/26; broad shell suite 186/203 with failures the worker attributes to its partial build (the reviewer must prove it). Strict MkDocs passes. Review staged. **No Opus workers running now.**
- **04:14** **W0 release prep.** Integrated-tree gates passed: full build, secret agent 9/9, network + Settings Network 39/39, validate-docs, strict MkDocs (laptop). Recorded in TASK_LIST/HANDOFF (`fc4a8054`), pushed main to the hub. Built archive `qindaqt-desktop-0.1.0_pre20260924.tar.gz` from `fc4a8054` (sha256 `86b7100b…`). New ebuild `gui-wm/qindaqt-desktop-0.1.0_pre20260924` (the W0 package; still allows qindatk ≥ r2 because it contains no KeyCap) + Manifest, overlay hub `dc78f18`; both machines' Portage overlays synced. Pretend-emerge clean. **Installing on qinda-top now.**
- **04:33** **W0 is live on qinda-top.** `gui-wm/qindaqt-desktop-0.1.0_pre20260924` installed through Portage (qcheck 1624/1624). Installed agent binary contains the fix (the new wire-form strings appear twice, as in the tested qinda build; the old pre-fix binary has none). Stopped the old agent; the session supervisor restarted it from the new binary (pid 64771, 04:33:05), and NetworkManager registered it at 04:33:06. **Note:** the supervisor restarts the agent only once per login, and this used it. **Still needs you:** join a password-protected network that isn't saved (or forget and rejoin one) and confirm the prompt appears; cancel once and confirm a second attempt prompts again.
- **04:33** Laptop working copy fast-forwarded to main `fc4a8054`. The superseded Sep 22 uncommitted Wi-Fi files are in `git stash` (`stash@{0}`) and on the hub branch `wip/qinda-top-network-secret-agent-20260922`.
- **05:14** **W6 repair done** (`08a2b990`, code `8b9cb788`): Preview holds an `org.freedesktop.ScreenSaver.Inhibit` request so the idle lock can't fire over it (fails closed if it can't get one; released on every exit); 60-second hard maximum; Blank must show on every display or it closes with a visible error; saver crash/exit status shown. Settings 133/133; strict MkDocs passes on the laptop. Recheck started (`luna-review-w6-recheck`), prioritised because it fixes Jarrod's reported problem.
- **05:46** **W1 review (Luna): REJECT**, two real findings (build and 135/135 fine): (1) while recording a shortcut in Input → Shortcuts, Ctrl+K opens search AND gets recorded; (2) the palette regroups results by section, so e.g. "screen" shows Screen saver and Login screen before the exact-keyword match Display. Non-blocking: the desktop ebuild must require QindaTK ≥ r4 for W1 (r5 once KeyCap ships). Repair started (`luna-w1-repair`).
- **06:06** **W6 ACCEPTED** on recheck (`08a2b990`). The reviewer verified on this machine that `org.freedesktop.ScreenSaver` is owned by kwin_wayland with KScreenLocker 6.6.6, whose idle-lock path really refuses to lock while inhibited and drops a caller's inhibition automatically when its D-Bus connection closes (Settings quit/crash). Settings 133/133.
- **06:06** **W6 integrated** on qinda main: merge `1f26b4da` + review records `aaa2c7ef`. Integrated gates running. Plan: ship W0 + W6 together as the next desktop revision so the screensaver fix doesn't wait for the rest. Started `luna-review-w23`.
- **06:15** **W6 release prep.** Integrated gates on main: full build, screensaver 6/6, Settings 133/133, secret agent 9/9, validate-docs; strict MkDocs on the laptop. Recorded in TASK_LIST/HANDOFF (`3dfec4c1`), pushed. Archive from `3dfec4c1` (sha256 `fd401888…`); ebuild `gui-wm/qindaqt-desktop-0.1.0_pre20260924-r1` (W0 + W6) + Manifest, overlay hub `8af3b00`; both overlays synced; pretend-emerge clean. **Installing on qinda-top.**
- **06:33** **W6 is installed on qinda-top** (`0.1.0_pre20260924-r1`, qcheck 1624/1624). The installed Settings binary no longer references `kscreenlocker_greet` and includes the `org.freedesktop.ScreenSaver` inhibit call; the Wi-Fi agent still carries the W0 fix. **For Jarrod:** the Settings window open since 23:28 is still the old program. Close Settings completely and reopen it, then try Screen saver → Preview for each saver and for Blank: no lock screen and no password; any key or mouse move returns to Settings; auto-lock still works afterwards.
- **06:53** **W1 repair done** (`fdeaa75f`, impl `8309c574`): while recording a shortcut, all Settings shell shortcuts are off and the recorder claims every key; filtered search results show as one "Results" list in rank order. Settings 135/135, Input 12/12. Caveat: the test can't send native Ctrl chords offscreen, so it drives the recorder's handlers directly. It also edits the same two Input shortcut files as W4's KeyCap change, so those will need reconciling at integration. Recheck staged.
- **06:53** Started `luna-review-w5` (network applet).
- **~07:39** **Codex usage limit hit, which resets 2026-09-29 15:49.** Both running Luna reviews (W2+W3 and W5) failed partway through with no verdict. GPT-6 Luna is unavailable until then. This also stops the other Codex jobs on qinda (the QindaTK lanes and the Sloom business heartbeat).
- **07:40** Reviews switched to Opus, two at a time: `opus-review-w1-recheck` and `opus-review-w23` (reusing Luna's nearly finished review build). The W5 review goes next.
- **07:50** New session. The two Opus reviewers (W1 recheck, W2+W3 review) stopped when the previous session ended; their progress is saved. **Jarrod asked for W7**: a desktop menu in the global menu when the desktop is selected or no window is active (like the Finder menu on macOS). Added to the plan; queued.
- **07:55** **Jarrod asked for the Finder integration program.** Added W8 (desktop icons below the top bar — cause verified: icon layout uses the full-screen rectangle), W9 (Applications like macOS), W10 (file manager right-click basics incl. Open With), W11 (tabs, Quick Look, remembered views, Recents), W12 (FileManager1 'Show in folder', always in the dock, desktop icons = File Manager view, system menu at top-left). W7 reframed: the no-app menu is the File Manager's menu, as Finder's is. Queue: W8 → W7 → W5 review → W9 → W10 → W11 → W12, two Opus at a time.
- **08:02** **W2+W3 review (Opus): REJECT, presentation only.** Measured: battery bars start at x = 244–377 depending on the row; Wi-Fi bars 153 px apart depending on trailing controls. Reviewer verified small fixes (equal grid columns; fixed-width trailing area) align them with tests still green. Everything else sound (unknown ≠ zero, warning thresholds match Power::WarningLevel, accessible names, skips justified; Settings 134/134). Repair sent back to the original W2+W3 worker (Opus), with screenshots required.
- **08:21** **W2+W3 repair done** (`ff3554c9`, fix `05566462`): equal-share columns for charge bars; fixed-width percentage + trailing area for Wi-Fi bars (new NetworkAccessPointActions.qml keeps QML block sizes in limits); alignment tests that fail on the old layout; EmptyState objectName checks; doc nit. Settings 134/134, network 36/36. Claude viewed the power and network screenshots: aligned. Recheck queued (note for reviewer: probe fixture shows a WPA2 row with prompt 'Open network.' — confirm it's fixture-only). **Started W8** (desktop icons below the top bar, Opus).
- **08:26** At Jarrod's request, workers now speak to him directly instead of Claude relaying (saves Claude's tokens). Two permanent voice seats, one per lane: `claude-helper-one` (Jessica, now the W1 re-check) and `claude-helper-two` (Harry, now W8). Each update starts with the worker's job name. Rule added to the worker and reviewer briefs.
- **08:56** **W8 candidate ready** (`c8d37bad`, plus `c2046e6f` test-isolation fix for a customize-menu test that already failed on main because it read the real ~/Desktop). Panel reach per screen edge → desktop work area; icons flow and drags stop inside it; stored positions untouched; ADR-0261. Desktop-surface 12/12; mutation checks show the tests catch the old behaviour. Claude viewed the screenshot: the top-bar band is empty, the first icon sits below it. **Started W7** (File Manager menu, Harry's lane). W8 review staged for Jessica's lane after the W1 re-check.
- **09:09** **W1 ACCEPTED** on Opus recheck (`fdeaa75f`): real key presses confirm capture records Ctrl+K/Ctrl+1/Alt+Left/Ctrl+Q and shortcuts return on every exit; results in one relevance-ordered list. Non-blocking follow-ups queued: Settings now builds the Input backend at launch (fix: bind it lazily); a latent stuck-shortcuts case on an Input sub-page swap; switch the test to real key presses; split the 511-line test file. **Integrated**: merge `11ecca24` (ADR index/mkdocs: kept 0257 and 0259), review records `32deef08`. Gates running. **Started the W8 review** (Jessica's lane).
- **09:10** **Jarrod waived independent review** ("I trust the agents that made it"). Stopped the W8 review. Candidates now integrate on the implementer's tests plus Claude's integration gates. Both lanes build: Harry = W7 (desktop menu), Jessica = W9 (Applications like macOS, started). Next for Claude: after the W1 gates finish, merge W8, W2+W3, W4 and W5 as a batch, run one combined gate, then release (desktop ebuild must require QindaTK r5).
- **09:15** W1 integrated gates passed (full build; search tests 2/2; Input 12/12; Settings 135/135; docs). **Batch merged onto main:** W8 `212af0a8`, W2+W3 `20c278d5`, W4 `4e3cf916`, W5 `e4505fd5` (only ADR index/mkdocs lines conflicted; resolved in number order). Combined gates running.
- **09:19** **Jarrod asked for W13**: drag-and-drop onto the dock/panels (apps, folders, files; reorder; drag off to remove) and dock groups that open upward like an accordion (e.g. an Office group). Verified today: no drop targets in any panel; the dock holds at most 16 app IDs in `panels.launcherPinned`. Added to the plan; next free lane after W7/W9.
- **09:20** **Jarrod asked for W14**: drag any applet on any panel to move it, within a panel and between panels (today: right-click → Move left/right one step at a time). The customization editor domain already implements drag previews and atomic cross-panel moves; only the Settings route uses them. W14 wires it into the live panels. W13 and W14 go next, one per lane.
- **09:21** W14 re-scoped per Jarrod ("in edit mode, they stop working normally, and I can just drag and drop them"). Found an existing edit mode with drag handles in code (PanelEditBar, AppletEditHandle); W14 now makes it discoverable and working end to end, especially cross-panel drags.
- **09:24** **Jarrod asked for W15**: Settings → Customize becomes a layout preset switcher (built-in + user presets: save current, rename, duplicate, delete; Modified/Restore original for edited built-ins); the WYSIWYG editor is removed and all editing happens on the panels. Hard gate: every capability of the Settings editor must exist on the panels first. Sequenced after W14.
- **09:26** **Jarrod: the Details view needs more views/customization; QindaTK is good for the FM.** W11 rescoped to 'File manager views on QindaTK' (Icons, customizable Details on Tk.DataTable, Columns, Gallery with Tk.Filmstrip, per-folder settings, preferences-v2); tabs/Quick Look/type-to-select/Recents split out as W16. Order after W15: W11 ∥ W10, then W16, W12.
- **09:31** Combined gates on the batch: build OK; Settings 135/136 → one load-sensitive screen saver test (3 s wait; needed 3.5 s) → test-only fix `6094e937` (10 s wait), 6/6 reruns; desktop surface 12/12; applets 16/16; network 44/44; docs OK; shell 199/205 with the known privileged-autostart staging limitation (`/etc/xdg/autostart/qindaqt-obs-login.desktop`, permission denied) and skipped virtual-desktop rows. Recorded (`6c728ce6`), pushed. **Release r2**: archive from `6c728ce6`, ebuild `qindaqt-desktop-0.1.0_pre20260924-r2` requiring QindaTK ≥ r5, overlay hub `ef6770e`; pretend clean; **installing on qinda-top**.
- **09:41** **Jarrod: his current laptop layout should be the default; fix the PlayStation-style window glyphs; drop the four-spoke logo.** Verified: active layout = stock `macos-inspired` (the `qindaqt.json` user copy is unused, so no edit to it is needed). Window buttons = style `glyph`: ✕ drawn with a dashed sketch pen; △ only on unmaximized windows (containers keep windows maximized, so it never shows); the four-spoke circle = `preferences-system`/`org.qindaqt.Settings` icons, used by Settings and the system-menu applet. Added **W17** (macos-inspired becomes the default and fallback) and **W18** (legible ✕; △ becomes the QindaQt system-menu mark; new Settings icon; four-spoke motif removed; screenshots for Jarrod's choice). Both small; next in line.
- **09:48** **Jarrod's theming requests** → **W19** (≈10 new button styles via one style-spec painter, legible ✕, more window/container decoration options; keep △=Maximize/□=Restore) and **W20** (XP-like and Windows 11-like experiences, legally distinct; new modern BeOS and Windows 3.1; NeXT refresh; iconify/roll-up where they fit; global menu only where it belongs; no Microsoft names). W18 narrowed to the logo/Settings icon. Constraint recorded: theming/config first, fewest code changes. **Mac-style experience stays as is** beyond requested items. **File manager styles** (Finder default / Explorer / Commander) folded into W11. Open question for Jarrod: should the Windows 11-like experience feel like Windows 11?
- **09:48** **Desktop r2 installed on qinda-top** (`0.1.0_pre20260924-r2`, qcheck 1625/1625; QindaTK r5). Running processes are still r1 until Jarrod logs out and back in. After that: Ctrl+K in Settings, Wi-Fi/battery bars, shortcut key caps, the network row in System Status, desktop icons below the top bar.
- **09:51** Jarrod logged out/in (now running r2). Session restarted; resumed W7 (Harry) and W9 (Jessica) from their saved worktrees.
- **09:52** Old helper sessions couldn't be resumed. Saved W7/W9 uncommitted work to hub snapshots (wip/claude-w7-desktop-menu-snapshot 477f7098; wip/claude-w9-applications-snapshot d978d5d5) and started fresh continuation workers in the same voice seats.
- **09:53** Jarrod decided: the Windows 11-like experience should feel like Windows 11, minus the advertising (no ads, promoted apps, web results or nags).
- **09:59** Jarrod added app pinning to W13 (Keep in Dock / Pin to Dock from running apps, launcher, start menu, Applications, desktop; one icon with a running indicator). Asset question answered: QindaQt draws its own theme assets; AI mockups optional for direction only.
- **10:28** **W9 integrated** (`b8617122`): build OK; file-manager 61/62 and Settings 135/136 under load, both failures pass alone 2/2 (load flakes). Pushed to hub. Both Opus helpers hit the usage limit; Jarrod reset it; resuming W7 and W17/W18.
- **10:34** **Jarrod changed the process:** for the remaining items (W19, W13, W20, W14, W15, W11, W10, W16, W12), write all the code first with peer code-reading, on one round branch (`round/code-first-20260924`); then one combined build and debug pass. W7 and W17/W18 (mid-build) finish normally first.
- **10:41** Process tuned for speed/tokens (Jarrod gave authority): three lanes (C = Adam: W19 → W12 → W20, started W19 now), per-file syntax checks allowed via a shared helper (≈1.5 s/file), no separate peer-review pass, lean briefs. Round branch `round/code-first-20260924` created at `b8617122`.
- **10:45** **W17/W18 done** (`fd95d15e`): macos-inspired is the schema/shell default (one constant), gear Settings icon (and six-tooth badge on Settings sub-icons), new 'Nest' system mark (alternatives Seed/Pebble in scratchpad w18-shots). Merged into the round branch (`a77e2c1a`) rather than main: one release at the end. **Started W13** (lane A, Jessica, code-first).
- **11:34** **W7 done** (`b2d45802`; tests 148/148; shell/settings 341/347 with only the known autostart staging failures). Merged into the round (`58871707`): W7's shared File Manager menu catalog now also carries W9's three Applications actions (syntax-checked). **Started W10** (lane B, Harry, code-first; told to add actions to the shared catalog). Round now = main + W17/W18 + W7.
- **15:22** All three helpers hit the Opus limit mid-writing; resumed after reset. All remaining assignments (W14, W15, W11, W11s, W16, W12, W20) pre-written so each lane moves on instantly.
- **15:28** **W19 written** (`4b9117fe`, code-first: 26 files syntax-OK, 11 need generated moc, 0 fail): one data-driven button painter; old styles pixel-locked by a test; 11 new styles (gel, bevel, blue-tiles, wide, tab, bold, minimal, pills, dots, outline, chunky); solid ✕○△□; 11 new decoration keys; roll-up uses the compositor's iconify (KWin 6.6 has no native shade). Merged into the round (`f7c305f1`). **Started W12** (lane C, Adam).
- **16:00** **W13 written** (`881dd524`; 33 ok / 10 need generated moc / 0 fail): panels.dockItems (apps, folders, files, groups, Trash) with migration from launcherPinned; drop/reorder/drag-off; category-named groups unfolding upward; one icon per running app; Pin/Keep in Dock across the shell; public Settings1DockPins helper for other processes. Merged into the round (`2d896244`; resolved BorrowedFacades field order and runtime initializer). **Started W14** (lane A, Jessica).
- **16:13** All three helpers hit the Opus limit (W10 mid-backend, W12 partway, W14 just started). Jarrod logged in again; resumed all three.
- **16:53** **W12 written** (`9d943fa9`; 19 ok / 9 need generated moc / 0 fail): org.freedesktop.FileManager1 (Show in folder) with a D-Bus activation file, desktop menus from the File Manager catalog, Keep in Dock in Applications, app drag to the dock. Merged into the round (`e8c8daec`, clean). W10 (still writing) told to fold W12 in (11 overlapping files; catalog ids for Open/Open With/New File/Get Info). **Started W20** (lane C, Adam). Note: Dolphin's FileManager1 activation file on qinda may win when no QindaQt FM is running; the session should prefer ours (for the final phase).
- **17:01** **W14 written** (`92d1ec10`; 18 ok / 5 need generated moc / 0 fail): Edit Panels in every panel/desktop menu and the desktop menu's View; applets inert while editing; cross-panel and cross-display drops by global pointer; gap + marker; Add applet…; Escape; bar no longer covers applets. Merged into the round (resolved DesktopContextMenu: both W12 and W14 gating kept). **Started W15** (lane A, Jessica).
- **17:14** **W10 written** (`83e745c0`; 40 ok / 10 need generated moc / 0 fail): full right-click sets, Open With via the Default Applications store, Open Terminal Here (QQ_Term), Compress/Extract via KArchive (zip-slip refused), and a fix for multi-item batches into one folder. Folded in W12/W14. Merged into the round. Packaging note: RDEPEND kde-frameworks/karchive:6. **Started W11** (lane B, Harry).
- **17:15** Started a low-priority warm-up build of the round branch (nice 19, -k 0) so the final build is incremental and all compile errors are collected in one pass; nobody acts on it until every item is written.
- **18:02** **W15 and W20 written and merged** (W15 `5c9e38ba`: preset gallery + parity additions; W20 `fa15e126`: six experiences, no vendor names; resolved tst_profiles + handbook merges). Warm build found ONE error round-wide (AUTOUIC: header named ui_*.h) → fixed by rename (`183e1572`); warm build 2 running. Lanes A and C finished; after W11, lane B does W11s+tabs and lane A does Quick Look/type-to-select/Recents in parallel.
- **20:30** **W11 written** (`db35cdf3`; 50 ok / 12 need generated moc / 0 fail): four QindaTK views, preferences-v2 per-folder views, Finder-like view inheritance. Merged (`7af46599`). Warm build 2 (everything through W20) had 0 failures. **Started W11s+tabs (Harry) and Quick Look/type-to-select/Recents (Jessica) in parallel** — the last two items.
- **20:55** **W16 written** (`68f2d57c`; 24 ok / 6 need generated moc / 0 fail): Quick Look (Space/Ctrl+Y, popover), type-to-select with spaces, Recents from recently-used.xbel. Merged into the round. Build 3 (with W11) running. Only W11s+tabs (Harry) left to write.
- **21:00** **Round builds clean** (everything except W11s): after a race-artifact link error and one test compile fix (QTest::keyClicks has no QWindow overload), full build 0 failures. Full ctest run started (excluding the known notification-live/desktop.virtual environment rows).
- **21:09** **W11s written** (`8c1a9d40`; 42 ok / 11 need generated moc / 0 fail): style setting (match layout / Finder / Explorer / Commander), per-tab navigation, folder tree, command bar, dual panes, F-key bar. Merged. **ALL ITEMS WRITTEN.** Build of W11s waits for the running ctest.
